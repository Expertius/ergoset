# Backup snapshot — 2026-04-15 15:30

Снимок состояния перед свободными правками пользователя.

## Коммит-родитель

`eead27e` — ui: landing content update, external images, leads service fix

## Что войдёт в следующий коммит

### Конфиг / DX

- **`.gitignore`**: добавлен `/.next-old/` (избежание сканирования тяжёлой папки Tailwind).
- **`postcss.config.mjs`**: `@tailwindcss/postcss` с `base: "./src"` — ограничение области сканирования классов.
- **`package-lock.json`**: служебное поле `peer: true` у одной из dev-зависимостей (npm lockfile drift).

### Документация

- **`docs/AI_CHANGELOG.md`**: запись от 2026-03-08 про ChunkLoadError / Turbopack hang и связанные правки.

### Данные

- **`e-station-image-urls.txt`**: список URL медиа с e-station.tech (новый неотслеживаемый файл).

## Примечание

В changelog упоминается правка `src/app/globals.css`; в диффе этой сессии её нет — либо уже в истории, либо запланирована отдельно.
